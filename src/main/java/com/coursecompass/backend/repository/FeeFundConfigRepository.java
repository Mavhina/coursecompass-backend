package com.coursecompass.backend.repository;

import com.coursecompass.backend.domain.FeeFundConfig;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FeeFundConfigRepository extends JpaRepository<FeeFundConfig, Long> {
}
